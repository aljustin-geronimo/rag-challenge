# Copy this file to .env and adjust as needed.

# If you're on Windows and Tesseract is not on PATH,
# set the full install path here (example below). Leave empty if not needed.
TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe

# Choose the embedding model (HuggingFace local model; no API key required)
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Optional: tweak top-k results for retrieval
TOP_K=5
